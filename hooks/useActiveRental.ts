// This hook was added for examples but isn't part of the minimal React Query setup.
// Keep a minimal stub to avoid breaking imports in other files — remove imports instead
// `useActiveRental` removed — it was created for demo purposes. Implement your own hooks to interact with APIs.
export {};
